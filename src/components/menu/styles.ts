import styled from "styled-components";

export const Wrapper = styled.div`
    /* width: 100vw; */
    padding: 1rem;
    position: fixed;
    bottom: 0;
    display: flex;
    justify-content: space-between;

`
